// @flow
import React from 'react';
// import PropTypes from 'prop-types';

const Nav = (props:any) => (
  <div className="Nav-wrapper">
    Test content
  </div>
);

Nav.PropTypes = {
  
}

Nav.defaultProps = {

} 

export default Nav;